name = "lumina"
compatibility_date = "2024-09-23"

# Vite 构建 viewer(多入口:index/gallery/404)到 dist/。
# manager 构建到 dist-manager/(仅本机,见 package.json 的 build:manager)。
[build]
command = "npm run build"

pages_build_output_dir = "./dist"
